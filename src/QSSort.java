/**
 * Interface QSSort defines a method called :sort(int[] input);
 * whatever classes implement this interface will define its own method inside
 */
public interface QSSort {
    public  void sort(int[] input);
}

/**
 * Interface Gen defines a method called :gen(int n,long m) return int [];
 * whatever classes implement this interface will define its own method inside
 */
public interface Gen {
    int [] gen(int n,long m);
}
